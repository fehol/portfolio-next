import Navbar from './Navbar';
// import { jsx, css } from '@emotion/react';

export default function header() {
  return (
    <header>
      <nav>
        <Navbar />
      </nav>
    </header>
  );
}
